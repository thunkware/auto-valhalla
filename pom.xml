<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.thunkware</groupId>
  <artifactId>auto-valhalla</artifactId>
  <version>0.1.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>auto-valhalla</name>
  <description>
    Java agent that rewrites eligible classes into JEP-401 value objects
    (Project Valhalla) at load time. Classes are selected via the
    @AutoValhalla annotation or system properties.
  </description>

  <url>https://github.com/thunkware/auto-valhalla.git</url>
  <inceptionYear>2026</inceptionYear>
  <organization>
      <name>thunkware</name>
  </organization>

  <modules>
    <module>auto-valhalla-api</module>
    <!-- the processor must be built before the maven plugin, which drives it -->
    <module>auto-valhalla-processor</module>
    <module>auto-valhalla-maven-plugin</module>
    <module>auto-valhalla-agent</module>
    <module>auto-valhalla-agent-attach</module>
    <!-- consumer end-to-end proofs for the Maven plugin -->
    <module>test</module>
  </modules>

  <properties>
    <maven.compiler.release>28</maven.compiler.release>
    <maven.compiler.enablePreview>true</maven.compiler.enablePreview>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
    <maven-source-plugin.version>3.2.1</maven-source-plugin.version>
    <maven-javadoc-plugin.version>3.11.2</maven-javadoc-plugin.version>
    <git-commit-id-plugin.version>4.0.5</git-commit-id-plugin.version>
    <spotless-maven-plugin.version>2.30.0</spotless-maven-plugin.version>
    <flatten-maven-plugin.version>1.8.0</flatten-maven-plugin.version>
  </properties>

  <licenses>
      <license>
          <name>Apache 2</name>
          <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
          <distribution>manual</distribution>
          <comments>A business-friendly OSS license</comments>
      </license>
  </licenses>

  <scm>
      <connection>scm:git:git://github.com/thunkware/auto-valhalla.git</connection>
      <developerConnection>scm:git:https://github.com/thunkware/auto-valhalla.git</developerConnection>
      <url>https://github.com/thunkware/auto-valhalla</url>
      <tag>HEAD</tag>
  </scm>

  <developers>
      <developer>
          <name>maxxedev</name>
      </developer>
  </developers>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven-jar-plugin.version}</version>
        <configuration>
          <archive>
            <manifestEntries>
              <Implementation-Title>${project.name}</Implementation-Title>
              <Implementation-Version>${project.version}</Implementation-Version>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>${maven-source-plugin.version}</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>${maven-javadoc-plugin.version}</version>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <doclint>none</doclint>
          <failOnError>false</failOnError>
          <!-- The JDK 5 modules (source/target 1.5) make the plugin's
               auto-detection of the JDK API link crash with
               "Index 0 out of bounds for length 0", so build the offline
               links explicitly instead. -->
          <detectJavaApiLink>false</detectJavaApiLink>
        </configuration>
      </plugin>

      <plugin>
        <groupId>pl.project13.maven</groupId>
        <artifactId>git-commit-id-plugin</artifactId>
        <version>${git-commit-id-plugin.version}</version>
        <executions>
          <execution>
            <id>get-the-git-infos</id>
            <goals>
              <goal>revision</goal>
            </goals>
            <phase>initialize</phase>
          </execution>
        </executions>
        <configuration>
          <prefix>git</prefix>
          <dotGitDirectory>${project.basedir}/.git</dotGitDirectory>
          <dateFormat>yyyy-MM-dd'T'HH:mm:ss.SSSZ</dateFormat>
          <generateGitPropertiesFile>true</generateGitPropertiesFile>
          <generateGitPropertiesFilename>${project.build.outputDirectory}/git-${project.artifactId}.properties</generateGitPropertiesFilename>
          <failOnNoGitDirectory>false</failOnNoGitDirectory>
          <includeOnlyProperties>
            <includeOnlyProperty>^git.branch$</includeOnlyProperty>
            <includeOnlyProperty>^git.build.time$</includeOnlyProperty>
            <includeOnlyProperty>^git.build.version$</includeOnlyProperty>
            <includeOnlyProperty>^git.commit.id.abbrev$</includeOnlyProperty>
            <includeOnlyProperty>^git.commit.time$</includeOnlyProperty>
            <includeOnlyProperty>^git.remote.origin.url$</includeOnlyProperty>
          </includeOnlyProperties>
        </configuration>
      </plugin>

      <!-- Declared at the root so the version and formatter config are shared,
           but skipped by default: only the agent module re-enables it (the JDK 5
           modules and demos are intentionally not formatted). -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless-maven-plugin.version}</version>
        <configuration>
          <skip>true</skip>
          <ratchetFrom>origin/main</ratchetFrom>
          <java>
            <importOrder />
            <trimTrailingWhitespace />
            <endWithNewline />
          </java>
        </configuration>
        <executions>
          <execution>
            <id>spotless-check</id>
            <phase>validate</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
